EL thread se crea y si no indicamos un destructor este queda en memoria

El join es como un wait. Si el padre no hace el join entonces tirará error. El join es obligatorio.
El join se puede hacer en cualquier momento. Al hacer join me quedo bloqueado hasta que termine el thread.

Los threads no son copiables. Si son movibles (transferencia)

Mutex

